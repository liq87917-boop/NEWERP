# 库存单据与库存成本说明（ERP-009）

> 任务：`ERP-009` 实现库存动作单据（盘点/调整、调拨、销售退货、采购退货）与库存成本基础。
> 范围：`src/ERP.Domain`、`src/ERP.Application`、`src/ERP.Infrastructure`、`src/ERP.Api`、`src/ERP.UnitTests`、`src/ERP.IntegrationTests`、`docs/**`、`.ai/**`。
> 不新增 / 不执行任何 SQL 脚本、不改 `deploy/**`、`release/**`、`checkpoints/**`、`logs/**`、`SeedData*.cs`；生产库、生产 OSS、正式部署与不可逆数据操作仍由 Human Gate 控制（本轮仅由 orchestrator 的真实 Edge 门禁在测试库验证）。

## 1. 背景与结论

ERP-006 审计把「库存动作单据（盘点/调拨/退货）+ 库存成本」判为 **missing**：`Stocks` 只有数量维度，单据只有采购入库 / 销售出库，且既有入库/出库审核只做数量增减、没有任何可审计的移动记录与成本口径。

ERP-009 的落地口径：

1. **四类单据全部由 EF 主子表承载**（`db_owner.StockAdjustments`、`db_owner.StockTransfers`、`db_owner.SalesReturns`、`db_owner.PurchaseReturns` 各自 + `...Details`），沿用报价单 / PI / 订单的既有约定（字轨编号、提交 → 审核、明细整体替换、后端复核合计）。
2. **每一次库存变动都必须有可审计的库存流水**（`db_owner.StockMovements`）：一笔记录 = 一个仓库 + 一个商品的单向变动，持久化成本单价 / 金额 / 移动后结存快照。
3. **销审 = 冲销，不删历史**：原流水标记 `IsReversed`，追加一条 `IsReversal` 红字流水（`ReversalOfMovementId` 指回原流水），库存按原金额还原；不安全的状态（库存已被后续业务占用）直接拒绝销审。
4. **成本口径为移动加权平均法**（moving weighted average），成本基准持久化，估价可逐笔核对：`Stocks.TotalCost` = Σ 流水金额，`Stocks.AverageCost` = `TotalCost / Quantity`。
5. **结构升级幂等**：`SchemaUpgrader.EnsureUpgradedAsync` 第 23 段以 `OBJECT_ID`（建表）+ `COL_LENGTH`（补列）双保险补齐，任一步可重复执行，表缺失时不会中断服务启动。

## 2. 数据模型

### 2.1 单据（`src/ERP.Domain/Entities/InventoryDocuments.cs`）

| 实体 | 关键字段 | 说明 |
|---|---|---|
| `StockAdjustment` / `StockAdjustmentDetail` | `AdjustmentNo`、`AdjustmentDate`、`WarehouseId/Name`、`AdjustType`、`TotalDiffQuantity`、`TotalDiffAmount`、`Status`；明细 `BookQuantity`、`ActualQuantity`、`DiffQuantity`、`UnitCost`、`DiffAmount` | 盘点/调整单：按「实盘 - 账面」差异改库存 |
| `StockTransfer` / `StockTransferDetail` | `TransferNo`、`TransferDate`、`FromWarehouseId/Name`、`ToWarehouseId/Name`、`TotalQuantity`、`TotalAmount`、`Status`；明细 `Quantity`、`UnitCost`、`Amount`、`BatchNo` | 仓库调拨：调出减、调入增，两侧同一成本单价 |
| `SalesReturn` / `SalesReturnDetail` | `ReturnNo`、`ReturnDate`、`CustomerId/Name`、`WarehouseId/Name`、`SourceStockOutId/No`、`ReturnReason`、`TotalQuantity`、`TotalAmount`；明细 `Quantity`、`UnitPrice`、`Amount`、`UnitCost` | 销售退货：客户退货重新入库，可按来源出库单溯源与取成本 |
| `PurchaseReturn` / `PurchaseReturnDetail` | `ReturnNo`、`ReturnDate`、`SupplierId/Name`、`WarehouseId/Name`、`SourceStockInId/No`、`ReturnReason`、`TotalQuantity`、`TotalAmount`；明细 `Quantity`、`UnitPrice`、`Amount`、`UnitCost` | 采购退货：退回供应商（库存减少），可按来源入库单溯源与取成本 |

### 2.2 库存流水 `StockMovement`

| 字段 | 类型 | 说明 |
|---|---|---|
| `MovementDate` / `MovementType` | datetime2 / `InventoryMovementType` | 移动日期与类型（PurchaseIn / SalesOut / Adjustment / TransferOut / TransferIn / SalesReturn / PurchaseReturn） |
| `SourceDocType` / `SourceDocId` / `SourceDocNo` | string(50) / long / string(50) | 来源单据（实体名 + Id + 单号），按单据即可追溯全部流水 |
| `WarehouseId` / `WarehouseName` | long / string(100) | 仓库（名称冗余，列表与流水页免关联） |
| `ProductId` / `ProductCode` / `ProductName` / `Spec` / `Unit` | long? / string | 商品信息（按商品 Id 补齐，前端只填名称时也完整） |
| `Direction` / `Quantity` | int / decimal(18,4) | 方向（1 入库 / -1 出库）与数量（恒为正数） |
| `UnitCost` / `Amount` | decimal(18,6) / decimal(18,4) | **成本基准**：本次移动单价与带符号金额（= 方向 × 数量 × 单价） |
| `BalanceQuantity` / `BalanceAmount` / `BalanceAverageCost` | decimal | 移动后结存数量 / 金额 / 加权平均成本快照 |
| `IsReversal` / `ReversalOfMovementId` / `IsReversed` | bit / long? / bit | 红字冲销标记、被冲销的原流水、原流水是否已被冲销 |

### 2.3 库存表成本列（`Stocks` 幂等补列）

| 列 | 类型 | 说明 |
|---|---|---|
| `AverageCost` | decimal(18,6) | 移动加权平均成本单价（历史数据默认 0，不影响既有功能） |
| `TotalCost` | decimal(18,4) | 库存金额（= 数量 × 加权平均成本，出库按该金额核减） |

> 小数位在 EF 模型中用 `HasPrecision` 显式声明（成本 6 位、数量与金额 4 位），与 `SchemaUpgrader` 第 23 段建表脚本一致，避免空库首次 `EnsureCreated` 按默认 `decimal(18,2)` 截断成本。

## 3. 库存成本口径（`InventoryService`，`src/ERP.Application/Services/InventoryService.cs`）

| 场景 | 规则 |
|---|---|
| 入库（调拨入库 / 退货入库 / 盘盈） | `TotalCost += 数量 × 成本单价`；`Quantity += 数量`；`AverageCost = TotalCost / Quantity`（数量为 0 时金额与均价一并归零） |
| 出库（调拨出库 / 采购退货 / 盘亏） | 成本单价缺省取**当前加权平均成本**；`Quantity < 需要数量` 时抛 `RuleConflict`（不落半截流水）；**成本下限保护**：出库成本不得超过该库存行账面金额，出库清零时按账面余额一次性核减，实际核减金额再折算回单价，保证流水金额恒等于库存金额变动（`Σ 流水金额 = Stocks.TotalCost`，账实可核对） |
| 调拨 | 以「调出流水」的成本单价作为「调入流水」的成本单价，因此**调出金额 = 调入金额**、两仓数量与金额双向守恒 |
| 退货成本优先级 | 明细指定 `UnitCost` → 来源单据流水成本（销售退货取来源出库流水、采购退货取来源入库流水）→ 当前加权平均成本 |
| 销审冲销 | 出库流水冲销按原金额加回库存；入库流水冲销按原金额从库存扣除，**数量不足或账面金额不足均拒绝销审**（不出现负库存、不出现负库存金额，`Σ 流水金额 = Stocks.TotalCost` 不被破坏），随后重算均价 |
| 取整 | 金额 4 位小数、成本单价 6 位小数（`MidpointRounding.AwayFromZero`），同一份单据重复计算得到相同结果 |
| 库存行唯一性 | 同一次工作单元（如同一张单据的多行明细）按「仓库 + 商品」复用同一行库存：新建行在 `SaveChanges` 之前先从 EF 变更跟踪器（`Stocks.Local`）复用，避免同仓同商品插入重复库存行、结存快照断裂与「单行库存不足」误判 |

## 4. 接口（`src/ERP.Api/Controllers`）

### 4.1 既有采购入库 / 销售出库的基础单位口径（ERP-023）

- `Stocks.Quantity` 与 `AvailableQuantity` 始终按商品 `BaseProduct.Unit`（基础单位）记账。
- 当入出库明细 `Unit` 等于商品 `PackageUnit` 且两者不同，保存前将 `Quantity × UnitsPerPackage`，并把明细单位改写为基础单位；因此审核、库存不足校验和取消冲回读取的是同一份持久化基础数量，不会二次换算。
- 未配置 `PackageUnit` 的商品、明细已使用基础单位、或历史未知单位均保持原数量，兼容既有数据。
- 明细明确使用 `PackageUnit` 时，`UnitsPerPackage <= 0` 或基础单位为空视为无效元数据并拒绝保存；不允许以错误换算因子静默记账。
- 已审核入库取消前会校验剩余库存是否足够冲回；已审核出库取消则按原基础数量恢复。未审核单据取消不改库存。
- **ERP-025 起**：审核与取消改为统一经 `InventoryService` 记账（见 §9），数量与金额口径不变，取消语义由「原路改库存」升级为「按库存流水生成红字冲销」（无流水可冲销的历史单据仍按原基础数量兜底冲回 / 恢复）。

### 4.2 API 路由

| 方法 | 路由 | 说明 |
|---|---|---|
| GET | `/api/inventory/stock-adjustments?keyword=&status=&warehouseId=&start=&end=` | 盘点单列表（关键字匹配单号 / 仓库名 / 备注） |
| GET | `/api/inventory/stock-adjustments/{id}` | 详情（含明细，按行号排序） |
| GET | `/api/inventory/stock-adjustments/{id}/movements` | 该单据的库存流水（含红字冲销行，按发生顺序） |
| POST / PUT | `/api/inventory/stock-adjustments[/{id}]` | 新增 / 修改（仅待提交可改；差异数量、差异金额、合计由后端复核） |
| POST | `/api/inventory/stock-adjustments/{id}/submit` · `/approve` · `/unaudit` · `/cancel` | 提交 → 审核（改库存 + 写流水）→ 销审（冲销）→ 取消（已审核需先销审） |
| GET/POST/PUT | `/api/inventory/stock-transfers…` | 调拨单（同上结构，另支持 `fromWarehouseId` / `toWarehouseId` 过滤） |
| GET/POST/PUT | `/api/inventory/sales-returns…` | 销售退货单（同上结构） |
| GET/POST/PUT | `/api/inventory/purchase-returns…` | 采购退货单（同上结构） |
| GET | `/api/stocks/movements?keyword=&warehouseId=&productId=&sourceDocNo=&movementType=` | 库存流水查询（关键字匹配来源单据号 / 商品编码 / 商品名称 / 仓库名） |
| GET | `/api/stock-ins/{id}/movements` | 采购入库单的库存流水（ERP-025，含红字冲销行，按发生顺序） |
| GET | `/api/stock-outs/{id}/movements` | 销售出库单的库存流水（ERP-025，含红字冲销行，按发生顺序） |

状态流转规则（四类单据一致）：

- `Pending --提交--> Submitted --审核--> Approved --销审--> Pending`；
- **仅已提交可审核**、**仅已审核可销审**、**仅待提交可修改 / 删除**；已审核不能取消（先销审）；
- 审核带**幂等护栏**：若该单据已存在有效流水（未冲销、非红字），重复审核直接拒绝，保证「已审核单据改库存恰好一次」。

## 5. 字轨与菜单

| 单据 | `DocumentType` | 前缀 | 菜单编码 / 路径 | 分组 |
|---|---|---|---|---|
| 库存盘点调整 | 19 | `PD` | `stock-adjustment` / `/logistics/stock-adjustment` | 库存管理（`logistics`） |
| 仓库调拨 | 20 | `DB` | `stock-transfer` / `/logistics/stock-transfer` | 库存管理 |
| 销售退货 | 21 | `XTH` | `sales-return` / `/logistics/sales-return` | 库存管理 |
| 采购退货 | 22 | `CTH` | `purchase-return` / `/logistics/purchase-return` | 库存管理 |
| 库存流水 | — | — | `stock-movement` / `/logistics/stock-movement` | 库存管理 |

菜单、授权（按同级菜单 `stock-in` / `stock-out` / `stock-query` 的角色自动授权）与单据号规则均由 `SchemaUpgrader` 第 23.6 段幂等写入；`app.js` 的 `CODE_ICON` 与 `modules-doc2.js` 的模块配置同步注册（无「该功能开发中」死链）。


## 6. 页面（`src/ERP.Api/wwwroot/js`）

- 四张单据页由 `crud.js` 主子表引擎驱动：仓库 / 客户 / 供应商为**引用字段**（可搜索选择），调整类型为下拉，明细行自动计算（调拨 / 退货为「数量 × 单价 = 金额」，盘点为「差异 = 实盘 - 账面、差异金额 = 差异 × 成本单价」并显示差异合计）。
- 行操作菜单新增通用「销审」（`unauditRow`，仅已审核行显示）+ 二次确认，销审后列表与库存流水同步刷新。
- 「库存管理 → 库存流水」为只读页：日期 / 类型 / 来源单据号 / 仓库 / 商品 / 方向 / 数量 / 成本单价 / 金额 / 结存数量 / 结存金额 / 冲销标记，支持按来源单据号检索。
- `crud.js` 本轮新增两项通用能力（不影响既有模块）：列表列 `type: 'map'`（枚举 / 布尔值渲染业务文案）、明细区 `detailDiff`（差异列自动计算与差异合计）。

## 7. 测试

- 单元测试（safe 档，`ERP.UnitTests`，新增 `InventoryMovementTests`）：差异计算与合计、审核按差异调整库存、重复审核拒绝、销审冲销（含红字流水与 `IsReversed`）、入库被占用后拒绝销审、已审核不能取消 / 删除、调拨数量与金额守恒、库存不足整单回滚、同仓调拨拒绝、销售退货按明细成本与来源出库成本计价、采购退货库存不足拒绝、移动加权平均（两次入库均价、出库按均价核减、数量归零金额归零）、流水成本基准与结存快照、字轨前缀、流水查询；另含本轮补充的账实一致用例：出库成本高于账面金额时按账面金额核减且销审精确还原、出库清零不留取整残额、入库成本已被后续业务占用时拒绝销审、同一商品多行明细只生成一条库存行且结存快照连续。
- 前端逻辑离线校验（临时 Node 脚本，仓库外运行，仅加载真实 `modules-doc2.js` + `crud.js` 并桩掉 DOM / 接口）：36 项断言覆盖模块注册、`unauditRow` 行操作、`detailDiff` 差异与合计重算、`map` 列渲染与金额格式。
- 真实浏览器验收（`ERP.IntegrationTests`，`Collection=UiTests`，Microsoft Edge，新增 `InventoryMovementUiTests`）：盘点单页面新建 → 审核 → 库存数量 / 金额 / 均价与流水一致 → 库存流水页按单据号可查；调拨单页面新建 → 审核 → 两仓数量与金额守恒、流水出现调拨出库 / 调入两笔；页面销审（确认框）→ 库存与流水冲销、重复销审被服务端拒绝且不产生重复效果、冲销后可再次提交审核；全流程断言无 JS / 接口失败（含 1 次有意的重复销审拒绝），并按场景输出截图证据。

## 8. 边界与未做项

- **持久化前提**：四张单据表与 `StockMovements` 由 EF 模型生成（`DbSet` 属性名即表名）；`SchemaUpgrader` 第 23 段用 `OBJECT_ID` 守卫补建，`Stocks` 的 `AverageCost` / `TotalCost` 用 `COL_LENGTH` 守卫补列，任一步幂等、可重复执行。
- 既有采购入库（`StockInController`）与销售出库（`StockOutController`）的基础单位换算与审核取消冲回由 ERP-023 落地；**ERP-025 已把两者接入库存流水**（审核写流水、取消红字冲销、幂等护栏、历史无流水单据兜底），见 §9。剩余未做：~~采购入库成本单价未从采购订单带入~~ → **ERP-033 已实现**（见 §11：唯一兼容订单行单价 → 基础单位成本；语义不明确时仍按「当前加权平均成本」兜底计价，首次入库为 0）。
- 存储过程版单据（`sp_Biz_StockIn` / `sp_Biz_StockOut`）本身不改库存，本轮未触碰任何存储过程与 SQL 脚本。
- 未做的成本能力：库位 / 批次级成本、先进先出（FIFO）、成本调整单、跌价准备；本轮只提供移动加权平均这一种确定性口径。
- 未执行任何生产 / 业务库操作，未执行 SQL、seed、部署；`deploy/**`、`release/**`、`checkpoints/**`、`logs/**`、SQL 文件、`SeedData*.cs` 均未改动。

## 9. ERP-025：采购入库 / 销售出库接入库存流水（2026-09-24）

> 任务：`ERP-025`「Record legacy stock in and out in movement ledger」，关闭 ERP-009 遗留的审计缺口：既有采购入库 / 销售出库审核只改 `Stocks`，不写 `StockMovements`，导致这两条最常用的库存路径不可追溯、库存金额也与「移动加权平均」口径脱节。
> 范围：`src/ERP.Api/Controllers`（`StockInController` / `StockOutController`）、`src/ERP.UnitTests`、`docs/**`、`.ai/**`；**不新增 / 不修改任何数据库结构**，未触碰 `SchemaUpgrader.cs`、SQL 脚本、存储过程、`deploy/**`、`release/**`、`appsettings*`。

### 9.1 做法

| 环节 | 口径 |
|---|---|
| 审核（采购入库） | `IncreaseAsync` + `InventoryMovementType.PurchaseIn`，`SourceDocType = StockIn`（单号 / Id / 仓库 / 商品随流水冗余落库），数量取明细**已换算的基础单位数量** |
| 审核（销售出库） | `DecreaseAsync` + `InventoryMovementType.SalesOut`，`SourceDocType = StockOut`，库存不足时由服务抛 `RuleConflict`（**整单不落半截数据**） |
| 幂等护栏 | 审核前 `CountActiveMovementsAsync > 0` 直接拒绝「已产生库存流水，不能重复审核」；单据状态护栏（仅已提交可审核）仍然生效，两层叠加保证「已审核单据恰好改一次库存」 |
| 取消（已审核） | `ReverseAsync`：原流水置 `IsReversed`、追加 `IsReversal` 红字流水（`ReversalOfMovementId` 指回原流水），库存数量与**库存金额**按原流水精确还原；重复取消被「已取消」拒绝 |
| 历史单据兜底 | 本次改造前审核、没有流水的单据：流水冲销返回 0 条时退化为按明细基础单位原路冲回 / 恢复，既不重复改库存，也不改写库存金额 |
| 明细守卫 | 零数量明细（旧版页面允许留空行）不产生库存变动；负数数量按数据错误拒绝（`InvalidParameter`） |
| 流水备注 | 采购入库「采购入库单审核入库（采购订单 Id N）」、销售出库「销售出库单审核出库（销售订单 Id N）」，便于按流水反查订单执行 |

### 9.2 成本口径与已知限制

- 入库明细（`StockInDetail`）与出库明细（`StockOutDetail`）都**没有成本列**，因此成本单价由 `InventoryService` 既有兜底口径决定：入库缺省取当前加权平均成本（首次入库为 0，不臆造采购成本），出库按移动加权平均成本核减并受「成本下限保护」约束。
- 采购订单价格按**包装单位**计价，直接套用会算错基础单位成本（需先按 `UnitsPerPackage` 折算），故本任务**不**从采购订单带入成本；如后续需要采购成本口径，应另立任务并同时定义包装单位折算规则。→ **ERP-033 已另立任务实现**：包装单位折算与币种口径见 §11，语义不明确时仍回退本节兜底口径。
- 效果：采购入库增加数量的同时按均价增加库存金额、销售出库按均价核减库存金额，`Stocks.AverageCost` 不再被单边变动拉偏，`Σ 流水金额 = Stocks.TotalCost` 的核对关系对「出入库 + 盘点/调拨/退货」全路径成立（库存行的历史基准金额除外）。

### 9.3 测试与验收

- 新增 `src/ERP.UnitTests/StockInOutMovementLedgerTests.cs`（11 用例）：入库写流水（方向 / 数量 / 成本 / 结存快照 / 来源单据号 / 备注）、首次入库成本 0、**重复审核幂等护栏**（含绕过状态护栏后仍被流水护栏兜住）、取消生成红字流水并冲回库存 + 重复取消拒绝、出库按均价核减并写流水、出库库存不足整单拒绝不写流水、出库重复审核拒绝、出库取消红字还原、历史无流水入库单 / 出库单取消兜底、包装单位入库流水数量为基础单位数量；另断言 `GET {id}/movements` 可按单据追溯（含红字行）。
- 既有 3 个测试文件（`StockInControllerTests`、`StockOutControllerTests`、`StockUnitConversionTests`）的构造函数调用同步注入 `IInventoryService`，原有断言（数量、状态、取消冲回）全部保持通过。
- 工程验证（safe 档）：`dotnet build NEWERP.sln -c Release --no-restore --no-incremental /p:TreatWarningsAsErrors=true /p:RunAnalyzersDuringBuild=true` → **0 警告 / 0 错误**；`dotnet test src/ERP.UnitTests/ERP.UnitTests.csproj -c Release --no-build` → **382/382 通过、0 失败**（2026-09-24 实测，含本任务新增 11 例）。
## 10. ERP-029：库存移动与呆滞报表（2026-09-24）

> 任务：`ERP-029`「Add inventory movement and stagnant-stock report」——在既有库存行与库存流水之上提供**只读派生**的「库存移动 + 呆滞」报表。
> 范围：`src/ERP.Application`（DTO / 报表服务）、`src/ERP.Api/Controllers`、`src/ERP.Api/wwwroot`、`src/ERP.UnitTests`、`docs/**`、`.ai/**`；**未新增或修改任何数据库结构**，未触碰 `SchemaUpgrader.cs`、SQL 脚本、存储过程、`deploy/**`、`release/**`、`appsettings*`、`SeedData*.cs`，也不写入任何业务数据。

### 10.1 数据与口径

| 项 | 口径 |
|---|---|
| 主表 | 库存行 `Stocks`（按仓库 + 商品）：`Quantity` 即当前现存量，是报表时点的权威库存数量 |
| 数量单位 | 一律为库存流水已落库的**基础单位数量**（ERP-023 换算后的结果）；报表不再做二次单位换算 |
| 最后移动日期 | 该（仓库 + 商品）在「移动日期 <= 截止日期」范围内的最大 `MovementDate`；无流水时为**未知（null）** |
| 入库 / 出库数量 | 移动窗口内的台账毛额：`Direction > 0` 合计 = 入库，`Direction < 0` 合计 = 出库（含红字冲销腿） |
| 净变动 | 入库 - 出库 = `Σ(方向 × 数量)`；**红字冲销流水以反方向记录**，原流水与其红字成对净额为 0，因此不做二次扣减、也不排除任何一侧 |
| 停滞天数 | 截止日期 - 最后移动日期（天）；无台账时为**未知（null）**，不臆造日期或比率 |
| 台账状态 | `ledger`（窗口内有移动）/ `window_empty`（有台账但窗口内无移动，此时出入库的 0 是**真实的 0**）/ `no_history`（截止日期前无任何流水） |
| 分类 | `stagnant`（停滞天数 >= 阈值）/ `active`（有台账且小于阈值）/ `unknown`（无台账，不判为呆滞、也不判为正常流动） |
| 成本 / 金额 | **不提供**：报表不估算库存成本或金额；库存金额口径仍以库存流水金额与库存行 `TotalCost` 为准 |
| 未知 vs 0 | 未知一律用 `null` 表达（前端显示「未知」），与真实的 0 严格区分 |

### 10.2 筛选与查询边界

- **筛选**：仓库（`warehouseId`）、商品（`productId`）、商品关键字（`keyword`，匹配商品资料编码 / 名称）、截止日期（`asOfDate`）、移动窗口（`windowStart` / `windowEnd`，闭区间且不得越过截止日期）、呆滞阈值（`inactiveDays`，必须 >= 1）、仅现存量 > 0（`onlyPositiveQuantity`，默认 true）、分页（`page` / `pageSize`，单页上限 200）。
- **口径截断**：截止日期之后的流水**不**进入最后移动日期 / 出入库 / 停滞天数（报表时点语义），但不删除台账。
- **有界查询**：基础行在数据库内排序（仓库 + 商品 + Id）后 `Skip/Take` 分页；仓库名与商品信息按「本页 keys」各一条批量查询；台账聚合按「本页 keys」两条分组查询（窗口内毛额 / 行数 / 红字行数，以及截止日期前最后移动日期）。正常使用下**不存在逐行查库**；合计与分类计数只统计本页（`scopeNote` 明确说明）。
- **商品筛选的可操作性**：页面上的「商品」是**商品资料下拉**（复用既有 `GET /api/base/products?page=1&pageSize=50&keyword=`），在关键字框输入编码 / 名称即联动刷新下拉内容，使用者不必先知道商品 Id；下拉只登记商品资料里的真实编码 / 名称（标签缺失时回落「商品 {Id}」），商品资料接口不可用时仅降级为「留空 / 只用关键字」，不阻断报表。报表接口本身仍按 `productId` 精确筛选，不做文本权威匹配、不臆造名称。
- **参数校验**：呆滞阈值 < 1、窗口开始晚于结束 → `BusinessException.InvalidParameter`（错误码 1001）；窗口结束晚于截止日期按截止日期截断；`pageSize` 超上限按 200 截断。

### 10.3 接口与前端

| 项 | 内容 |
|---|---|
| 接口 | `GET /api/reports/inventory-movement?warehouseId=&productId=&keyword=&asOfDate=&windowStart=&windowEnd=&inactiveDays=&onlyPositiveQuantity=&page=&pageSize=` |
| 服务 | `ReportService.GetInventoryMovementReportAsync`（`src/ERP.Application/Services/ReportService.Inventory.cs`，只读，不 `SaveChanges`） |
| DTO / 口径常量 | `src/ERP.Application/Interfaces/InventoryMovementReportDtos.cs`（`InventoryMovementReportQuery` / `InventoryMovementItem` / `InventoryMovementReport` / `InventoryMovementSemantics`，口径文案单点定义，前后端与测试共用） |
| 页面 | `src/ERP.Api/wwwroot/js/inventory-movement-report.js`：五项筛选（商品为**商品资料下拉**，关键字框同时筛选下拉内容）+ KPI + 明细表 + 分页 + 导出当前页 CSV；未知统一显示「未知」 |
| 入口 | 库存查询页（`modules-doc2.js` 的 `stock-query` 模块）工具栏「📉 库存移动 / 呆滞报表」；`index.html` 注册脚本 |

### 10.4 测试与验收（本轮实测）

- 新增 `src/ERP.UnitTests/InventoryMovementReportTests.cs`（15 用例）：活跃库存（基础单位数量 / 最后移动日期 / 出入库 / 停滞天数 / 台账状态）、呆滞阈值边界（90 天恰好呆滞、89 天正常，且分类随请求阈值变化）、无台账行显式「未知」（日期与停滞天数均为 null、不判呆滞、文案含「不臆造」）、红字冲销成对净额（入库 10 + 冲销出库 10、出库 4 + 冲销入库 4 → 毛额各 14、净额 0、红字行数 2、`未做二次扣减`）、移动窗口（窗口内无移动 = 真实的 0，但最后移动日期与停滞天数仍取窗口外台账）、截止日期截断（之后的 99 不入账，时点后移后重新纳入）、仓库 / 商品 / 关键字 / 仅现存量 > 0 筛选、分页有界与分页合计、参数校验与归一化、报表不含任何成本 / 金额字段、`ReportController` 端点返回 `ApiResponse`、前端接线（入口函数 / 接口路径 / 筛选控件 / **商品资料下拉** / 脚本注册），另加 3 例：
  - **有界查询与只读**：用只读计数上下文代理（`DispatchProxy`，不改生产代码）断言「与行数无关」——库存行 1 行（分页 1）为**恒定 5 次**数据集访问（库存行 + 窗口台账 + 历史台账 + 仓库名 + 商品信息），扩到 301 行（分页 200）后访问次数仍为 5 次；关键字 EXISTS 查询在整页 200 行与单行时访问次数一致；全程 `SaveChanges` 调用为 0。
  - **窗口闭区间边界**：窗口首日带时间部分与末日带时间部分的移动都计入，窗口前一天的移动不计入；`MovementCount` / 最后移动日期 / 停滞天数一致。
  - **红字跨窗口边界**：原入库在窗口外、红字冲销在窗口内时，窗口内只计红字腿（入库 0 / 出库 10 / 净额 -10、红字行数 1），报表**不跨窗口配对、不二次扣减**，红字流水同样作为台账事实参与最后移动日期。
- 工程验证（safe 档）：`dotnet build NEWERP.sln -c Release --no-restore --no-incremental /p:TreatWarningsAsErrors=true /p:RunAnalyzersDuringBuild=true` → **0 警告 / 0 错误**；`dotnet test src/ERP.UnitTests/ERP.UnitTests.csproj -c Release --no-build` → **461/461 通过、0 失败、0 跳过**（2026-09-24 实测，含本任务这 15 例）。
- 浏览器 / UI 验收按开发阶段策略记为 **`browser_deferred`**（延后到 `FINAL-UI-ACCEPTANCE`）：延后场景为「打开库存移动报表，按仓库与日期筛选并核对基础单位移动合计」「设置呆滞阈值并核对呆滞行与无台账行分别呈现为独立状态」；本任务未运行真实 Edge、`Collection=UiTests`、截图 / 视觉回归，也未启动 API、未连接业务库、未执行任何 SQL / seed / 部署。

### 10.5 边界与未做项

- 报表以**库存行**为主表：只有台账、没有对应库存行的历史组合（例如库存行被软删除）不在本报表范围内；本任务为只读报表，不修补、不新建库存行。
- 现存量取库存行当前值，报表**不提供历史时点的结存快照**（`StockMovements.BalanceQuantity` 可逐笔核对，但本报表不做任意时点的余额重建）。
- ~~不做库龄分层（按批次 / 首次入库日）、不做成本估值与跌价准备、不做周转率 / 周转天数~~ → **库龄分层与成本估值已由 ERP-034 实现**（见 §12：台账 FIFO 分层 + 库存行持久化加权平均成本，库龄未知与成本未知单列）；仍未做：按批次 / 库位 / 首次入库日的分层、跌价准备、周转率与周转天数；呆滞判定只依据「最后移动日期」这一个台账事实与请求阈值。

## 11. ERP-033：采购入库成本单价从采购订单带入（2026-09-24）

> 任务：`ERP-033`「Carry authoritative Purchase Order cost into stock-in movements」——关闭 ERP-025 §9.2 遗留的成本口径缺口：采购入库明细没有成本列，审核时一律按「当前加权平均成本」兜底（首次入库为 0），采购订单已谈定的价格无法进入库存成本。
> 范围：`src/ERP.Api/Controllers`（`StockInController` + 新增 `PurchaseStockInCost.cs`）、`src/ERP.UnitTests`、`docs/**`、`.ai/**`；**未新增 / 未修改任何数据库结构**，未触碰 `SchemaUpgrader.cs`、SQL 脚本、存储过程、`deploy/**`、`release/**`、`appsettings*`、`SeedData*.cs`，不重写任何历史库存与历史流水。

### 11.1 判定口径（`PurchaseStockInCost` / `PurchaseStockInCostSource`）

| 判定项 | 规则（任一项不满足即回退，绝不臆造） |
|---|---|
| 权威链接 | 只用入库单自身持久化的 `StockIn.PurchaseOrderId`；不按商品名 / 供应商 / 备注文本猜测 |
| 订单可用性 | 订单必须存在、未软删除且 `Status = Approved`（待提交 / 已提交 / 已驳回 / 已取消订单的价格不构成成本依据） |
| 订单行唯一性 | 该订单在本商品上必须**有且仅有一行**明细：零行 = 缺证据，多行 = 价格不唯一，二者均**不任选一行** |
| 商品元数据 | `BaseProduct` 必须存在且未删除（否则无法确认基础单位与装箱数） |
| 单位语义 | 订单行单位 = 商品基础单位 → 直接作为基础单位单价；订单行单位 = 商品装箱单位（且与基础单位不同）→ 单价 ÷ `UnitsPerPackage`（折算因子必须 > 0）；其他 / 空单位一律回退。**只折算单价，不改动任何已落库数量**（数量口径仍由 ERP-023 决定） |
| 币种语义 | 库存行与库存流水没有币种列，历来以人民币计价：`CNY` 订单直接使用单价；外币订单只有在订单持久化了**非占位汇率**（`ExchangeRate > 0` 且 ≠ 1，1 是 `init*.sql` 与 `BillProcController.LoadCurrencyDefaultsAsync` 的「未配置」占位）时才按该汇率换算为人民币，否则回退，**绝不臆造汇率** |
| 单价可用性 | 折算后的基础单位成本必须 > 0（保留 6 位小数，与 `InventoryService.RoundCost` 同一取整口径），空 / 零 / 负单价一律回退 |
| 回退口径 | 任一项不满足时 `UnitCost = 0`，交由 `InventoryService.IncreaseAsync` 既有兜底计价（当前移动加权平均，首次入库 0），**与 ERP-025 行为完全一致** |

### 11.2 落地方式

| 环节 | 口径 |
|---|---|
| 判定位置 | 审核（`StockInController.Approve`）一次性 `PurchaseStockInCostSource.LoadAsync`：固定 2 次查询（本单商品资料 + 链接订单含明细），随后逐行 `Resolve` 为纯内存判定，无逐行查库 |
| 记账 | `IncreaseAsync(context, d.Quantity, cost.UnitCost)`：订单价 > 0 时作为入库成本参与移动加权平均；为 0 时行为与 ERP-025 一致 |
| 可追溯 | 流水备注写入成本来源：「采购入库单审核入库（采购订单 Id N，成本来源：采购订单单价 / 采购订单单价（按装箱数折算为基础单位）/ 采购订单单价（按订单汇率换算）/ 既有移动加权平均兜底（原因））」；未链接采购订单时备注保持 ERP-025 原文案不变 |
| 冲销 / 幂等 | 取消仍走 `ReverseAsync` 红字冲销（红字沿用原流水单价、金额取反）、重复审核仍被「已产生库存流水」护栏拒绝；**不改写任何历史流水**（原流水的数量 / 单价 / 金额保持原值，仅置 `IsReversed`） |
| 未做的结构变更 | 不新增成本列、不落库成本来源字段、不引入汇率主数据、不做价税分离——单价一律取订单行 `UnitPrice` 原值 |

### 11.3 测试与验收（本轮实测）

- 新增 `src/ERP.UnitTests/PurchaseStockInCostTests.cs`（17 个方法 / 26 个用例）：唯一订单行按订单单价计价并参与移动加权平均（`Σ 流水金额 = Stocks.TotalCost`）、包装单位订单价按装箱数折算为基础单位成本且数量不变、两次不同订单价入库的加权均价、无链接保持既有兜底与原文案、订单不存在 / 未审核（Pending · Submitted · Cancelled · Rejected）/ 已软删除、订单行缺失、同商品多行不任选价格、订单行单位无法折算（未知单位 / 空单位 / 装箱数 0 / 装箱数负）、订单行零与负单价、商品资料缺失、外币按订单持久化汇率换算、外币汇率为占位 1 / 0 / 负数时不换算、取消红字冲销不改写历史流水、重复审核幂等护栏与取消后重试，以及控制器 / 解析器接线静态断言。
- 工程验证（safe 档）：`dotnet build NEWERP.sln -c Release --no-restore --no-incremental /p:TreatWarningsAsErrors=true /p:RunAnalyzersDuringBuild=true` → **0 警告 / 0 错误**；`dotnet test src/ERP.UnitTests/ERP.UnitTests.csproj -c Release --no-build` → **528/528 通过、0 失败、0 跳过**（2026-09-24 实测，含本任务这 26 例）。
- 浏览器 / UI 验收按开发阶段策略记为 **`browser_deferred`**（延后到 `FINAL-UI-ACCEPTANCE`）：延后场景为「审核一张链接单一采购订单行的入库单，核对基础单位成本来源（流水单价 / 金额 / 备注成本来源）」「审核一张币种或订单行不可用的入库单，核对仍按既有加权平均兜底且不臆造换算」；本任务未运行真实 Edge、`Collection=UiTests`、截图 / 视觉回归，也未启动 API、未连接业务库、未执行任何 SQL / seed / 部署。

### 11.4 边界与未做项

- 只按「商品 + 唯一订单行」匹配：**不按订单行序号 / 批次 / 规格配对**，同一商品在订单上拆多行（不同交期或规格）时视为价格不唯一并回退。
- 到货数量与订单数量不做比例分摊：单价是订单行的计量属性，部分收货不改变单价。
- 不做价税分离（含税 / 不含税）、不做关税 / 运费等采购附加成本分摊、不做外币折算差额，也不引入汇率主数据表。
- 成本只在**审核那一刻**解析并持久化到流水；此后订单价格被修订不会回改已落账流水（历史成本不可变）。
- 销售出库成本仍按移动加权平均核减（ERP-009 口径），本任务不引入销售侧成本来源。

## 12. ERP-034：库存库龄与成本估值报表（2026-09-24）

> 任务：`ERP-034`「Add inventory aging and supported valuation report」——补齐 ERP-029 §10.5 明确未做的「库龄分层与成本估值」，做成只读派生报表。
> 范围：`src/ERP.Application`（新增 `InventoryAgingReportDtos.cs` 与 `ReportService.InventoryAging.cs`）、`src/ERP.Api/Controllers/ReportController.cs`、`src/ERP.Api/wwwroot/js/inventory-aging-report.js`（+ 库存查询页入口与脚本注册）、`src/ERP.UnitTests`、`docs/**`、`.ai/**`；**未新增 / 未修改任何数据库结构**（无新表、无新列、无 SQL 脚本、无存储过程），未触碰 `SchemaUpgrader.cs`、`deploy/**`、`release/**`、`appsettings*`、`SeedData*.cs`，不改写任何历史库存与历史流水。

### 12.1 库龄口径（数量）

| 环节 | 规则 |
|---|---|
| 主表 | 库存行（`Stocks`）：现存量即报表时点的权威基础单位数量（与 ERP-029 同一主表口径，不做事后单位换算） |
| 分层（FIFO） | 方向 `+1` 的入库流水按 `MovementDate` 与数量**形成一层**；方向 `-1` 的出库流水按「层日期（同日按流水 Id）」**从最早的分层开始消耗**，剩余分层即现存量的库龄依据 |
| 分层边界 | 按「截止日期 − 层日期」天数归入 **0-30 / 31-60 / 61-90 / 91-180 / 180 天以上**（均为闭区间：30 天在 0-30、31 天在 31-60、90 天在 61-90、91 天在 91-180、180 天在 91-180、181 天在 180 天以上） |
| 时点截断 | 一律「`MovementDate <= asOfDate`」（含截止当天带时间部分的流水）；截止日期之后的流水**不进入口径也不被删除**，时点后移后重新纳入 |
| 红字冲销 | 按 `ReversalOfMovementId` **权威配对**：冲销一张入库 → 扣回该入库**自己的那一层**（该层已被后续出库消耗的部分按 FIFO 扣减）；冲销一张出库 → 原出库并未发生，按**原出库的移动日期与数量**回补一层（数量与库龄都回到原状态，而不是按冲销日重新起算）；原流水不删除、不改写、不二次扣减 |
| 原流水不可见 | 配对依据缺失（原流水已删除 / 不在口径内）时按红字腿**自身方向**兜底（`+1` 成层、`-1` 参与 FIFO 消耗），不猜测配对关系；此时不计入 `reversalCount` |
| 库龄未知 | `现存量 − 台账分层合计 > 0` 的差额（流水起点之前的历史库存）单列为「库龄未知」，**不臆造入库日期、也不放进任何分层** |
| 台账多于现存量 | `台账分层合计 > 现存量` 时按 FIFO 口径从**最早分层**扣减，记入 `ledgerDeficitQuantity` 并在行说明留痕（不新增 / 不改写流水） |
| 缺证据的消耗 | 出库 / 红字腿没有可扣减的分层时记入 `unpairedQuantity` 并留痕，不臆造分层 |
| 库龄依据状态 | `full`（现存量全部有分层依据）/ `partial`（部分数量无依据）/ `none`（截止日期前无任何流水） |

### 12.2 估值口径（金额）与币种

| 环节 | 规则 |
|---|---|
| 行权威金额 | 取库存行持久化的 `Stocks.TotalCost`（= Σ 库存流水金额，ERP-009 口径）：本报表不重算、不重建历史成本、不计提跌价准备 |
| 分层金额 | `分层数量 × Stocks.AverageCost`（持久化移动加权平均成本，金额 4 位小数、`AwayFromZero`）；**取整残差并入数量最多的一格**，使「5 个分层金额之和 + 库龄未知金额 = 行权威金额」恒成立（测试逐行断言该恒等式） |
| 成本未知 | `AverageCost` 或 `TotalCost` 为 0（首次入库的兜底成本 / 历史库存）→ `costStatus = unknown`：金额一律为 **`null`（未知，不是 0）**，数量单列 `unknownCostQuantity`，**排除在任何权威合计之外**，既不估算成本也不回落为 0 |
| 币种 | 库存行与库存流水**没有币种列**，持久化成本历来以人民币计价（ERP-009；ERP-033：外币采购价只有订单持久化了非占位汇率换算后才进入库存成本）→ 报表金额合计全部是同一币种（`costCurrency = CNY`），**不做跨币种合并，也不从文本数据字典（`BaseOtherInfo`）或系统参数推断 / 套用汇率**（单元测试用只读计数上下文断言报表未访问这两个数据集） |

### 12.3 筛选、有界查询、接口与前端

| 项 | 内容 |
|---|---|
| 筛选 | `warehouseId` / `productId` / `keyword`（匹配商品资料编码 / 名称）/ `asOfDate`（截止日期，缺省今天）/ `onlyPositiveQuantity`（默认 true）/ `page` / `pageSize`（默认 50，上限 200，超出按上限截断；`page < 1` 归一为 1；关键字去空白后为空则不过滤） |
| 有界查询 | 库存行在数据库内排序（仓库 + 商品 + Id）后 `Skip/Take` 分页；仓库名、商品信息、库龄台账**各一次**批量查询且只按「本页 keys」，正常使用下**不存在逐行查库**；合计、分层合计与计数只统计本页（`scopeNote` 明确说明） |
| 接口 | `GET /api/reports/inventory-aging?warehouseId=&productId=&keyword=&asOfDate=&onlyPositiveQuantity=&page=&pageSize=` |
| 服务 | `ReportService.GetInventoryAgingReportAsync`（`src/ERP.Application/Services/ReportService.InventoryAging.cs`，只读，不 `SaveChanges`） |
| DTO / 口径常量 | `src/ERP.Application/Interfaces/InventoryAgingReportDtos.cs`（`InventoryAgingSemantics` 口径文案与分层常量、`InventoryAgingReportQuery`、`InventoryAgeBucketItem`、`InventoryAgingItem`、`InventoryAgingReport`——后端派生、前端展示与测试断言共用同一份口径） |
| 页面 | `src/ERP.Api/wwwroot/js/inventory-aging-report.js`：仓库下拉 + 商品资料下拉（关键字联动）+ 截止日期 + 仅现存量 > 0 + 每页；KPI + 「本页分层合计」表（数量 / 金额）+ 明细表（5 个分层的数量与金额、库龄未知、分层金额合计、权威金额、成本单价、成本状态、库龄依据、说明）+ 分页 + 导出当前页 CSV；未知统一显示「未知」 |
| 入口 | 库存查询页（`modules-doc2.js` 的 `stock-query` 模块）工具栏「⏳ 库存库龄 / 成本估值报表」；`index.html` 注册脚本（**未改菜单表与授权数据**，只读报表挂在既有库存查询页） |
| 报表字段 | 行级：`currentQuantity` / `knownAgedQuantity` / `unknownAgeQuantity` / `evidenceStatus` / `averageCost` / `costStatus` / `authoritativeAmount` / `agedAmount` / `unknownAgeAmount` / `unknownCostQuantity` / `ledgerDeficitQuantity` / `unpairedQuantity` / `reversalCount` / `buckets` / `note`；页级：`pageCurrentQuantity` / `pageKnownAgedQuantity` / `pageUnknownAgeQuantity` / `pageAuthoritativeAmount` / `pageUnknownCostQuantity` / `knownCostCount` / `unknownCostCount` / 三种库龄依据计数 / `pageBuckets` / `rule` / `costRule` / `scopeNote` |

### 12.4 测试与验收（本轮实测）

- 新增 `src/ERP.UnitTests/InventoryAgingReportTests.cs`（19 个用例）：
  - **库龄边界**：同一行 9 个分层（0 / 30 / 31 / 60 / 61 / 90 / 91 / 180 / 181 天）分别落入 0-30（2）、31-60（2）、61-90（2）、91-180（2）、180 天以上（1），并核对分层键 / 中文标签 / 上下界与金额。
  - **FIFO 消耗**：三层（200 / 100 / 10 天）出库 10 → 最早那层被消耗完，剩余 100 天与 10 天两层各 10。
  - **库龄未知**：无台账行（现存量 7、零流水）全部分层为 0、库龄未知 7、依据 `none`、金额 `null`、文案含「库龄未知 / 不臆造入库日期」；部分无依据行（现存量 15、台账 10）→ 31-60 = 10、库龄未知 5、依据 `partial`，并断言「分层金额 + 库龄未知金额 = 行权威金额」。
  - **红字冲销配对**：冲销「10 天前」那张入库（而不是最早那层）→ 200 天那层保留、被冲销的层扣回（确保不是简单 FIFO 消耗）；冲销一张出库 → 按**原出库日期**（31-60）回补，而不是按冲销日（0-30）；原流水不可见时按红字腿自身方向兜底且不计入 `reversalCount`。
  - **已知 / 未知成本分离**：成本已知行金额 30、成本未知行金额 `null` 且数量单列 5，页级权威合计只含成本已知行（30），只筛未知成本行时页级权威金额与分层金额均为 `null`。
  - **确定性取整**：持久化均价 1.00005、两格各 1 件 → 取整合计 2.0002 与权威金额 2.0001 的 -0.0001 残差并入数量最多的一格，恒等式仍成立。
  - **截止日期截断**：截止当天（带时间部分）计入、之后的 5 件为库龄未知；时点后移到 3 天后重新纳入并归属 0-30。
  - **缺证据与台账差额**：出库大于任何分层 → `unpairedQuantity = 2` 且不臆造分层；台账 20 > 现存量 12 → 差额 8 按 FIFO 从最早分层扣减并留痕。
  - **筛选 / 分页 / 校验**：仓库 / 商品 / 关键字 / 仅现存量 > 0 筛选（含零数量行标注、无匹配返回空集）；分页行序与「合计只统计本页」；`pageSize` 上限截断；查询归一化（缺省截止日期、`page` 钳制、关键字去空白）。
  - **非变更**：库存行与台账逐列前后一致（含 `IsReversal` / `ReversalOfMovementId`），只读计数上下文全程 `SaveChanges` 为 0。
  - **有界查询**：1 行（分页 1）为**恒定 4 次**数据集访问（库存行 + 仓库名 + 商品信息 + 库龄台账），扩到 301 行（分页 200）后访问次数不变；`StockMovements` 数据集只读一次；关键字 EXISTS 查询在整页 200 行与单行时访问次数一致；且未访问 `BaseOtherInfo` / 系统参数数据集（币种口径）。
  - **接口 / 前端接线**：`ReportController.InventoryAging` 返回 `ApiResponse` 载荷；离线静态断言前端入口函数、接口路径、筛选控件、商品资料下拉、脚本注册与菜单入口。
- 工程验证（safe 档）：`dotnet build NEWERP.sln -c Release --no-restore --no-incremental /p:TreatWarningsAsErrors=true /p:RunAnalyzersDuringBuild=true` → **0 警告 / 0 错误**；`dotnet test src/ERP.UnitTests/ERP.UnitTests.csproj -c Release --no-build` → **547/547 通过、0 失败、0 跳过**（2026-09-24 实测，含本任务这 19 例）。
- 浏览器 / UI 验收按开发阶段策略记为 **`browser_deferred`**（延后到 `FINAL-UI-ACCEPTANCE`）：延后场景为「打开库存库龄报表并按截止日期核对 5 个分层与库龄未知数量」「核对成本未知行的数量与金额分别呈现为独立状态（金额显示『未知』而不是 0）」；本任务未运行真实 Edge、`Collection=UiTests`、截图 / 视觉回归，也未启动 API、未连接业务库、未执行任何 SQL / seed / 部署。

### 12.5 边界与未做项

- 库龄是**按台账 FIFO 分层的派生口径**，不是对库存行做批次 / 库位级追踪：没有批次（`BatchNo`）、库位、生产日期 / 有效期维度，也不按「首次入库日」单值判定库龄。
- 同一仓库 + 商品仅有一个库存行与一条 FIFO 队列：调拨、退货、盘点等单据只要是台账流水就同等参与分层，不区分单据类型优先级。
- 估值只用库存行持久化的移动加权平均成本与库存金额：**不做跌价准备 / 可变现净值、不做库龄越久的成本调整、不做周转率与周转天数、不做历史时点余额重建**（`StockMovements.BalanceQuantity` 仍可逐笔核对）。
- 报表只读：不落库、不改库存、不改单据状态、不新增或修改任何表列，也不补齐或重建历史流水；「库龄未知」与「成本未知」都只如实呈现，不做任何推断或估算。
- 页面未加入报表中心菜单表（`reports.js` / 菜单数据未改），入口仍复用库存查询页工具栏（与 ERP-029 同一入口位置）。





