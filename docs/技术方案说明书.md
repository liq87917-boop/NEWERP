# 外贸 ERP 系统 · 技术方案说明书

> 版本：V1.1 · 日期：2026-09-23 · 项目名：NEWERP
>
> **V1.1 补正说明（ERP-017）**：按 ERP-006 全仓审计结论，把本文档的技术栈、目录结构、API 清单、测试方案与自动化控制层同步到 ERP-006 ~ ERP-010 的实测状态。本次仅补文档，**未改动任何业务代码、SQL、部署产物**；文中的测试数字为 2026-09-23 在本机 Release 配置下的实测值。

## 一、技术栈选型

| 层次 | 技术 | 版本 | 说明 |
|---|---|---|---|
| 语言/框架 | C# / ASP.NET Core WebAPI | .NET 8 LTS | Windows Server 2022 完全兼容 |
| 数据库 | SQL Server | 2022 | 地址、端口和数据库名通过部署环境变量配置 |
| ORM | EF Core | 8.0.8 | Code-First，启动时 EnsureCreated 自动建表 |
| 认证 | JWT Bearer | 8.0.8 | 无状态令牌 + 角色/权限声明 |
| 日志 | Serilog | 8.0.1 | 文件按天滚动 + Console 双输出 |
| 测试（单元） | xUnit + EF InMemory | 2.9.0 / 8.0.8 | `ERP.UnitTests`：默认安全档唯一执行的测试工程（272/272 通过） |
| 测试（集成 / UI） | xUnit + Microsoft.Data.SqlClient + Selenium.WebDriver | 2.9.0 / 5.2.0 / 4.27.0 | `ERP.IntegrationTests`：数据库集成用例 + Microsoft Edge UI 用例（`Collection=UiTests`，受门禁、默认不执行） |
| API 文档 | Swashbuckle | 6.7.0 | OpenAPI 调试界面 |
| 映射 | 手动 DTO / 实体直传 | — | 不过度抽象，简单实体直接暴露 |

## 二、项目目录结构

```
NEWERP/
├── NEWERP.sln
├── .clinerules                        # 自动化开发安全规则（保护路径 / 默认安全验证档）
├── .env.example / .env.local          # 环境变量模板 / 本机开发变量（真值不入库）
├── .gitattributes / .gitignore
├── start-dev.ps1 / start-deploy.ps1 / start_agent.bat   # 开发启动 / 发布产物启动 / 常驻 Agent 控制台
├── .ai/                               # 自动化控制层（GPT 对话控制的事实源）
│   ├── tasks/ERP-NNN.json             # 任务定义：验收标准 / allowed_paths / depends_on / 验证档 / Human Gate
│   ├── results/                       # 每个任务的执行结果（含 finish_reason，如 browser_deferred）
│   ├── evidence/ERP-NNN/<run>/        # 浏览器验收证据：TRX + browser-session.json + 截图 + SHA-256 清单
│   ├── decisions/ control/ logs/      # Human Gate 决策、控制面状态、runner 日志
│   ├── MASTER_PLAN.md / GPT_CONTROL_PROTOCOL.md / FUNCTION_BACKLOG.md
│   ├── PROJECT_STATE.json / config.json / audit.jsonl / pipeline.lock
│   └── prompts/developer.md           # 每次注入 Cline 的开发约束
├── docs/                              # 交付文档：技术方案说明书 / 数据库设计说明书 / 部署交付文档 / 订单追溯字段说明 /
│                                      #           库存单据与库存成本说明 / 报价单与PI设计方案 / 菜单与业务流程优化建议 / AUTOMATED_DEVELOPMENT
├── scripts/                           # 自动化编排（Python + PowerShell）
│   ├── ai_orchestrator.py             # 单任务编排：注入任务 → Cline → 工程验证 → 结果与审计 → Git checkpoint
│   ├── ai_pipeline.py                 # 队列命令：queue / defer / retry / retry-push / 连续执行
│   ├── ai_validate.py                 # 按验证档执行工程验证（默认 safe = Release build + ERP.UnitTests）
│   ├── ai_browser_acceptance.py       # 真实 Edge 验收驱动（延后阶段不运行）
│   ├── gpt_project_control.py / gpt-control.ps1
│   └── run-ai.ps1 / run-pipeline.ps1 / resume-pipeline.ps1 / status-ai.ps1 / new-ai-task.ps1 / test-pipeline.ps1 / agent-host.ps1 / agent-bootstrap.ps1
├── tools/                             # 传统交付辅助脚本：gen-init4.ps1 / apply-init4.ps1 / smoke-test.ps1
├── src/
│   ├── ERP.Domain/                    # 领域层：实体 + 枚举（零依赖）
│   ├── ERP.Application/               # 应用层：DTO、接口、服务（认证 / 单据号 / 通用 CRUD / 报表 / 库存成本）、统一响应
│   ├── ERP.Infrastructure/            # 基础设施：ErpDbContext、SchemaUpgrader、StoredProcedureService、JWT、Excel 导入导出、OSS、种子数据、DI
│   ├── ERP.Api/                       # API：Controllers、中间件、配置、wwwroot 前端（原生 HTML/CSS/JS，无构建工具）
│   ├── ERP.UnitTests/                 # 单元测试（默认安全档；2026-09-23 实测 272/272 通过）
│   └── ERP.IntegrationTests/          # 数据库集成测试 + Selenium Edge UI 用例（Collection=UiTests，受门禁，默认不执行）
├── deploy/                            # 发布产物 + init.sql / init2~16.sql + web.config（受保护路径）
├── release/ checkpoints/              # 发布包与 checkpoint 备份（受保护路径）
└── logs/                              # 构建 / 测试 / 冒烟日志（受保护路径）
```

## 三、分层架构

**Controller → Service → Repository/DAO 严格分层：**

- **ERP.Domain**：纯 POCO 实体（继承 `BaseEntity` 审计基类）+ 枚举，无任何依赖。
- **ERP.Application**：定义 `IErpDbContext` 数据访问抽象接口（解耦）、`IAuthService`/`IDocumentNumberService`/`IGenericService<>`/`IReportService` 服务接口及其实现，统一响应 `ApiResponse<T>`、错误码、业务异常。
- **ERP.Infrastructure**：`ErpDbContext : DbContext, IErpDbContext` 具体实现，`JwtTokenService`，`SeedData` 种子数据，`DependencyInjection` 注册。
- **ERP.Api**：RESTful 控制器（含 `BaseCrudController<>` 泛型 CRUD 基类、`DocumentControllerBase<>` 单据基类），全局异常中间件、操作日志中间件。

## 四、统一接口规范

所有 API 统一响应 `{ code, message, data }`；分页 `{ items, total, page, pageSize, totalPages }`。

**错误码规划：**

| code | 含义 | HTTP |
|---|---|---|
| 0 | 成功 | 200 |
| 1000 | 通用业务错误 | 400 |
| 1001 | 参数校验失败 | 400 |
| 1002 | 数据不存在 | 404 |
| 1003 | 数据已存在/重复 | 409 |
| 1004 | 业务规则冲突 | 409 |
| 2000 | 未认证 | 401 |
| 2001 | 用户名或密码错误 | 401 |
| 2002 | 权限不足 | 403 |
| 2003 | 令牌过期 | 401 |
| 2004 | 账号被禁用 | 403 |
| 5000 | 服务器内部错误 | 500 |

## 五、API 接口清单（按模块）

### 认证
- `POST /api/auth/login` 登录
- `GET /api/auth/profile` 当前用户信息（角色/权限/菜单树）
- `POST /api/auth/change-password` 修改密码

### 系统设置（/api/sys）
- `users` 用户管理（含重置密码、分配角色）
- `roles` 角色管理（含分配菜单）
- `menus` 菜单管理（树形）
- `parameters` 系统参数
- `document-number-rules` 单据号规则
- `client-limits` 客户端限制
- `logs` 系统日志

### 基础资料（/api/base）
- `customers` 客户、`suppliers` 供应商、`employees` 员工、`expense-accounts` 费用科目、`warehouses` 仓库、`products` 商品、`other-infos` 其他资料（币种/港口/货代/唛头等）
- `customers` 客户资料支持可选「指定货代」（ERP-036）：复用「其他资料」中 `InfoType = Forwarder` 的字典项，
  仅存引用 Id（`ForwarderId`）+ 服务端写入的名称快照（`ForwarderName`），刻意不建外键、不做生产回填；
  下拉选项由 `GET /api/base/customers/forwarder-options` 提供（只返回未删除、已启用、类型为 Forwarder 的字典项），
  新增 / 更换引用时服务端校验字典项有效性并覆盖客户端提交的自由文本；字典项后来被停用 / 删除时，
  客户列表与详情仍显示当时的名称快照，并以 `ForwarderAvailable = false` 标注「已停用/不可用」（不可再被新选中）。
  该字段仅作主数据指引：不会自动写入订舱 / 装柜 / 报关 / 费用单据，也不涉及任何外部货代系统。

### 询价管理（/api/inquiries）
- 询价单 CRUD + 提交/审核/取消 + 导出

### 订单管理
- `/api/sales-orders` 销售订单 CRUD + 提交/审核/取消 + 导出
- `/api/purchase-orders` 采购订单 CRUD + 提交/审核/取消 + 导出

### 物流管理
- `/api/stock-ins` 采购入库（审核后增加库存）
- `/api/stock-outs` 销售出库（审核后校验并扣减库存）
- `/api/stocks` 库存查询

### 装柜管理（/api/container）
- `receiving-plans` 收货计划、`bookings` 订柜信息、`pre-loadings` 预装柜单、`loading-lists` 装柜清单

### 账务管理（/api/finance）
- `deposit-applies` 定金申请、`payment-applies` 货款申请、`payments` 付款单、`container-settlements` 装柜结算、`bulk-settlements` 散货结算、`receipts` 收款单、`complaints` 客诉单

### 报表管理（/api/reports）
- `product-sales-ranking` 商品销量排名榜
- `order-profit` 订单利润暂估表
- `customer-shipment` 客户出货量统计表
- `salesman-output` 业务员产值报表
- `balance-sheet` 资产负债表
- `income-statement` 利润表
- `cash-flow` 现金流量表

### 询报价与销售链（阶段 3 · PI 由 ERP-007 交付 · 转订单由 ERP-010 交付）
| 方法 | 路由 | 说明 |
|---|---|---|
| GET / POST / PUT | `/api/sales/quotations`、`/api/sales/quotations/{id}` | 报价单主子表 CRUD（金额与合计服务端复核） |
| GET | `/api/sales/quotations/from-inquiry/{inquiryId}` | 由询价单带入客户与明细 |
| POST | `/api/sales/quotations/{id}/approve` · `/unaudit` | 审核 / 销审 |
| POST | `/api/sales/quotations/{id}/to-pi` | 报价单转 PI（同一报价单仅一次） |
| GET / POST | `/api/sales/quotations/{id}/order-prefill` · `/{id}/to-order` | 带入预填销售订单 / 直接生成销售订单（来源留痕、重复守卫） |
| GET | `/api/sales/quotations/{id}/print` | 报价单打印数据（打印走 `sales-pi.js` + 打印模板 `/api/sys/print-templates/{billType}`） |
| GET / POST / PUT | `/api/sales/proforma-invoices`、`/api/sales/proforma-invoices/{id}` | PI 主子表 CRUD（银行信息 / 收货人 / 通知人 / 唛头 / 定金） |
| POST | `/api/sales/proforma-invoices/{id}/approve` · `/unaudit` · `/void` | 审核 / 销审 / 作废 |
| GET / POST | `/api/sales/proforma-invoices/{id}/order-prefill` · `/{id}/to-order` | PI → 销售订单（同时留痕 PI 背后的报价单） |
| GET | `/api/sales/proforma-invoices/{id}/print` | PI 打印数据 |

### 订单（EF 主子表，ERP-008 起）
- `/api/sales-orders`、`/api/purchase-orders`：列表（关键字含客户 PO 号 / 合同号 / 归属销售订单号）、详情、新增 / 修改、状态流转、`/{id}/print`、`export-excel`。订单已改由 EF 主子表承载，存储过程版单据页退出菜单（`/api/v2/sales-orders` 仅为历史数据排查保留）。

### 库存动作单据与库存流水（/api/inventory、/api/stocks，ERP-009）
- `/api/inventory/stock-adjustments` 盘点/调整、`/api/inventory/stock-transfers` 调拨、`/api/inventory/sales-returns` 销售退货、`/api/inventory/purchase-returns` 采购退货：分页 / 详情 / 新增 / 修改 / 提交 / 审核 / 销审 / 取消 + `/{id}/movements`（该单据的库存流水）。
- `/api/stocks` 库存查询、`/api/stocks/summary` 汇总、`/api/stocks/movements` 库存流水（关键字、仓库、商品、来源单据号、移动类型）。

### 阶段 1~3 其他已交付模块
| 模块 | 前缀 | 说明 |
|---|---|---|
| 费用单 | `/api/finance/expenses` | 出口杂费台账 + 拼柜分摊（预览 / 生成 / 重复防护） |
| 出口退税台账 | `/api/base/tax-refunds` | 单表台账（通用 CRUD） |
| 供应商比价 | `/api/purchase/quotes` | 同一 `QuoteNo` 多供应商横向比价 |
| 单证中心 | `/api/trade/documents` | 出口单证台账（自动生成 / Excel 导出具 ERP-019，**未实现**） |
| 客户跟进 CRM | `/api/crm/follow-ups` | 跟进记录 + 下次跟进日期 |
| 样品管理 | `/api/crm/samples` | 打样 / 寄样 / 样品费 |
| 钉钉通知 | `/api/sys/dingtalk` | 跟进提醒推送与发送记录（`SysDingTalkLogs`） |
| 代理服务费协议证据（ERP-069） | `/api/agency-service-fee-agreements` | 客户代理服务费**商业条款证据**台账：分页 / 详情 / 新增草稿 / 修改草稿 / 登记 / 作废（原因必填）；条款只来自用户显式提交，比例费率与固定金额互斥，不从提成设置或主数据比例推断；不开票、不记账、不授权付款 |
| 代理服务费对账单证据（ERP-070） | `/api/agency-service-fee-statements` | 客户代理服务费**操作性对账证据**台账：分页 / 详情（含行清单）/ 元数据 / 来源候选（有界）/ 新增草稿 / 修改草稿 / 登记 / 作废（原因必填）；行只按**来源类型 + 持久化来源 Id** 显式引用销售订单或装柜清单（绝不按单号文本 / 金额 / 相似度猜链接），必须显式关联一条**已登记**的 ERP-069 协议（客户与币种一致），行金额与计费基础只来自显式填写、**合计由服务端计算**、到期日留空即未知；同一来源只能被一条未作废对账单行引用；不开票、不记账、不收款、不催收、不联系客户 |

### 单据通用能力（/api/v2/bills，第四阶段）
| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/v2/bills/{billType}` | GET | 分页查询（关键字=单据号、状态筛选） |
| `/api/v2/bills/{billType}/navigate?oid=&direction=` | GET | 首张/上一张/下一张/末张导航 |
| `/api/v2/bills/{billType}/{oid}` | GET | 详情（主表 + 副表明细） |
| `/api/v2/bills/{billType}/save` | POST | 新增/保存（Oid=0 为新增，自动取单据号） |
| `/api/v2/bills/{billType}/{oid}/{op}` | POST | 状态流转：`audit` 审核、`unaudit` 销审、`void` 作废、`restore` 还原、`delete` 删除 |
| `/api/v2/bills/{billType}/{oid}/logs` | GET | 单据操作日志（按单据号追溯；oid=0 查询该单据类型全部日志） |
| `/api/v2/bills/{billType}/export` | GET | 导出 Excel（关键字/状态/日期范围） |
| `/api/v2/bills/{billType}/import-template?withSample=` | GET | 下载导入模板（withSample=true 附带示例行） |
| `/api/v2/bills/{billType}/import` | POST | 上传 Excel 逐行导入（逐行调用存储过程 Save，返回成功/失败明细） |

> ⚠️ 状态流转路由变量为 `{op}`：MVC 中 `action` 属保留路由值，使用 `{action}` 会导致 405。

### 打印设计（/api/sys/print-templates，第四阶段）
| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/sys/print-templates?billType=` | GET | 模板列表 |
| `/api/sys/print-templates/{billType}` | GET | 取默认模板（无模板时返回内置默认配置） |
| `/api/sys/print-templates` | POST | 新增/更新模板（同类型同名唯一；设默认时自动取消其它默认） |
| `/api/sys/print-templates/{id}` | DELETE | 软删除模板 |

### 基础资料导入导出（/api/base/io，第四阶段）
| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/base/io/{resource}/export` | GET | 导出全部记录（resource：customers/suppliers/employees/expense-accounts/warehouses/products/other-infos） |
| `/api/base/io/{resource}/import-template` | GET | 下载导入模板 |
| `/api/base/io/{resource}/import` | POST | 上传 Excel 导入（编码重复逐行拦截） |

### 系统日志（/api/sys/logs，第四阶段增强）
- 支持 `module`、`userName`、`billNo`、`start`、`end`、`keyword`（模糊匹配路径/单据号/用户/动作/模块）筛选。

## 六、第四阶段前端能力（打印 / 导入导出 / 日志）

| 文件 | 说明 |
|---|---|
| `js/bill-print.js` | 单据三件套：打印预览（弹窗内渲染打印版式）、打印（新窗口 + `@page` 纸张/字号 + 调起浏览器打印）、打印设计（模板配置：抬头/标题/纸张/字号/页脚/字段勾选与排序，保存到 `SysPrintTemplates`）；同时提供单据导入/导出按钮逻辑与「操作日志」弹窗 |
| `js/base-io.js` | 基础资料三件套：列表打印预览与打印、打印设计、Excel 导入（模板下载/文件上传/结果明细）、导出、操作日志查看 |
| `js/bill-v2.js` | 列表行操作新增「销审 / 打印预览 / 打印 / 日志」按钮；工具栏新增「导入 / 导出 / 打印设计 / 操作日志」 |
| `js/crud.js` | 基础资料工具栏新增「导入 / 导出 / 打印预览 / 打印 / 打印设计 / 操作日志」 |
| `js/bill-edit.js` | 单据编辑页 Hero 区新增「销审 / 打印预览 / 打印 / 打印设计」按钮 |

**打印版式说明**：公司抬头 + 单据标题 + 单据号/打印时间 + 主表字段两列网格 + 明细表 + 备注 + 签署栏 + 页脚；
纸张支持 A4 / A5 / A4 横向 / 80mm 小票，字号 8–24px，字段顺序与是否打印明细/备注均由模板控制。

## 七、开发辅助脚本（tools/）

> 自动化开发编排脚本不在 `tools/` 而在 `scripts/`（任务队列、验证、Git checkpoint、浏览器验收、GPT 控制面），详见第十一节；`tools/` 保留第四阶段的传统交付辅助脚本。

| 脚本 | 说明 |
|---|---|
| `tools/gen-init4.ps1` | 从 init2.sql / init3.sql 提取 16 个 `sp_Biz_*`，以「BEGIN/END 嵌套计数」方式注入销审分支并生成 `deploy/init4.sql`（避免人工维护 16 份存储过程） |
| `tools/apply-init4.ps1` | 读取 `appsettings.Development.json` 连接串，按 GO 分批执行 `deploy/init4.sql` |
| `tools/smoke-test.ps1` | 冒烟测试：自动拉起/复用 WebAPI，逐项验证打印模板、单据增删改查、审核/销审、日志、导入导出、基础资料导入导出，并清理测试数据；日志输出 `logs/smoke-yyyyMMdd.log` |


## 八、单据状态机

所有业务单据统一状态：`待提交(Pending) → 已提交(Submitted) → 已审核(Approved)`，另有 `已驳回(Rejected)`、`已完成(Completed)`、`已取消(Cancelled)`。状态流转通过 `DocumentControllerBase` 统一封装。

## 九、测试方案

**单元测试（xUnit + EF InMemory）覆盖：**
1. 密码哈希/校验（PBKDF2）
2. 单据号生成（前缀 + 日期 + 流水号递增）
3. 通用 CRUD（增删改查、软删除、分页）
4. 报表聚合逻辑
5. Excel 导入解析与模板生成（中文表头映射、数字/日期单元格、空行跳过）
6. 导入值规范化（系统字段忽略、币种文本、千分位金额、是否字段）
7. 打印模板（默认模板回退、新增/同名冲突、切换默认、软删除）
8. 操作日志查询（模块、单据号、关键字、时间范围筛选、软删除过滤）

**测试命令（默认安全档）：** `dotnet test src/ERP.UnitTests/ERP.UnitTests.csproj -c Release --no-build` → **272/272 通过、0 失败、0 跳过**（2026-09-23 实测；同批次 `dotnet build NEWERP.sln -c Release` 为 0 警告 0 错误）。

**近期新增覆盖（阶段 3 与 ERP-007 ~ ERP-010）：** 报价单转 PI（`QuotationToPiTests`）、PI 主子表与状态机（`ProformaInvoiceControllerTests`）、订单追溯字段与打印 / 导出（`OrderTraceabilityTests`）、库存单据与移动加权平均成本 · 冲销（`InventoryMovementTests`）、报价 / PI 转销售订单（`SalesOrderConversionTests`）、验收目标库安全护栏（`TestDatabaseSafetyGuardTests`）、菜单与字轨（`MenuControllerTests` / `BillCatalogTests`）。

**近期新增覆盖（ERP-043 ~ ERP-070 证据登记册与只读派生视图）：** 供应商采购发票（`PurchaseInvoiceTests`）、业务单据附件引用 / 内容证据（`DocumentAttachmentReferenceTests` / `AttachmentEvidenceTests`）、销售订单变更申请（`SalesOrderChangeRequestTests`）、供应商付款引用与付款 → 采购发票引用（`SupplierPaymentAllocationTests` / `SupplierPaymentInvoiceAllocationTests`）、客户收款引用（`CustomerReceiptAllocationTests`）、客户销项发票证据（`CustomerSalesInvoiceEvidenceTests`）、装柜出运引用 / 里程碑（`ContainerShipmentReferenceTests` / `ContainerShipmentMilestoneTests`）、供应商对账与账龄（`SupplierReconciliationAgingTests`）、代理服务费协议证据（`AgencyServiceFeeAgreementTests`，ERP-069，39 例）、**代理服务费对账单证据（`AgencyServiceFeeStatementTests`，ERP-070，50 例）**。

**最近一次安全档实测（ERP-070，2026-09-25）：** `dotnet build NEWERP.sln -c Release --no-restore --no-incremental /p:TreatWarningsAsErrors=true /p:RunAnalyzersDuringBuild=true` → 0 警告 / 0 错误；`dotnet test src/ERP.UnitTests/ERP.UnitTests.csproj -c Release --no-build` → **1543/1543 通过、0 失败、0 跳过**。

**受门禁的测试（默认不执行）：** `src/ERP.IntegrationTests`（数据库集成用例）与 `Collection=UiTests`（Selenium + Microsoft Edge）必须先满足目标库安全护栏（`ERP_AI_TEST_RUN=1` 且 `ASPNETCORE_ENVIRONMENT=Development`，或人工批准的测试上下文）才可运行；浏览器验收按 `browser_deferred` 延后到 `FINAL-UI-ACCEPTANCE` 阶段。

**冒烟测试命令：** `powershell -ExecutionPolicy Bypass -File .\tools\smoke-test.ps1`（日志：`logs\smoke-yyyyMMdd.log`）

完整表结构见《数据库设计说明书.md》。

## 十、前端交互组件

前端为原生 HTML/CSS/JS（无构建工具），静态资源位于 `src/ERP.Api/wwwroot`，随发布产物一同输出到 `deploy/wwwroot`。

**日期选择器（`js/datepicker.js` + `css/datepicker.css`）**

| 项目 | 说明 |
|---|---|
| 目的 | 替换浏览器原生 `<input type="date">` 外观，统一为符合 UI 设计规范的日历浮层 |
| 接入方式 | 页面加载后自动接管所有 `input[type=date]`；弹窗/侧滑面板等动态渲染的表单由 `MutationObserver` 自动接管，业务代码零改动 |
| 取值约定 | 输入框 `value` 始终保持 `yyyy-MM-dd` 文本，`saveForm`/`saveBill` 仍按原逻辑拼接 `T00:00:00` 提交 |
| 功能 | 日/月/年三级视图切换、上下年月导航、今天·本月初·本月末快捷、清空、周一为周起始、今天与选中态高亮、`min`/`max` 范围禁用 |
| 键盘操作 | `↑/↓` 增减一天，`PageUp/PageDown` 增减 30 天，`Enter` 确认并规范化，`Esc` 收起 |
| 手工录入 | 兼容 `2026-8-6`、`2026/8/6`、`20260806` 等写法，失焦自动规范化；非法输入回退为上一次有效值 |
| 对外 API | `window.ErpDatePicker.{enhance, enhanceAll, open, close, format, parse}` |

> 若后续新增页面使用日期输入，只需照常写 `<input type="date">` 即可自动获得该样式与交互。

## 十一、自动化开发控制层（.ai + scripts）

| 项 | 说明 |
|---|---|
| 控制模型 | GPT 对话为控制面，仓库文件为事实源：任务（`.ai/tasks`）、状态（`.ai/PROJECT_STATE.json`）、结果（`.ai/results`）、证据（`.ai/evidence`）、决策（`.ai/decisions`）、审计（`.ai/audit.jsonl`） |
| 任务生命周期 | `pending → in_progress → 工程验证 → code_ready → completed → Git checkpoint → push`；`depends_on` 组成校验过的无环图，队首非终态任务阻塞队列，禁止隐式跳过 |
| 滚动队列 | `rolling_queue.batch_size = 4`、低水位 2，由 GPT 从 `.ai/FUNCTION_BACKLOG.md` 补充下一批；队列清空时进入 `replenishing` 等待新批次 |
| 验证档 | `safe`（Release build + `ERP.UnitTests`，默认）、`build_only`、`integration`（数据库集成）、`ui`（Edge UI）；本文档补正任务 ERP-017 使用 `build_only` |
| 完成模式 | 业务任务默认 `completion_mode: browser`；开发阶段浏览器门禁按 `defer_browser_during_development` 延后并记 `browser_deferred`（延后到 `FINAL-UI-ACCEPTANCE`）；仅控制层任务可用 `control_plane` |
| Human Gate | 生产库 / SQL / `SchemaUpgrader` / `SeedData` / 部署发布 / 不可逆操作需人工批准；L3/L4 永不自动批准 |
| 保护路径 | `.env*`、`src/ERP.Api/appsettings*.json`、`deploy/**`、`release/**`、`checkpoints/**`、`user_input_files/**`、`logs/**`、`**/*.sql`、`SchemaUpgrader.cs`、`SeedData*.cs` |
| 运行命令 | `scripts/status-ai.ps1` 查看状态；`scripts/run-ai.ps1` / `scripts/run-pipeline.ps1` 执行；`scripts/resume-pipeline.ps1` 续跑；`py -3 scripts/ai_pipeline.py queue` 查看队列；常驻控制台 `start_agent.bat` |
| 证据要求 | 浏览器验收须产出 TRX + `browser-session.json` + 截图 + SHA-256 清单；开发阶段延后不等于放弃，最终 UI 验收阶段必须补齐 |

## 十二、交付文档索引

| 文档 | 内容 |
|---|---|
| 《技术方案说明书.md》（本文） | 技术栈、目录结构、分层架构、接口规范与清单、测试方案、自动化控制层 |
| 《数据库设计说明书.md》 | 基线表结构 + 阶段 1~3 与 ERP-007 / 008 / 009 新增表与列（第十 ~ 十四节） |
| 《部署交付文档.md》 | 启动方式、环境变量、数据库初始化、IIS 部署、逐批交付记录（含 §32 本次自动化交付批次） |
| 《订单追溯字段说明.md》 | ERP-008：销售 / 采购订单外贸与追溯字段、接口、页面、校验、边界 |
| 《库存单据与库存成本说明.md》 | ERP-009：四类库存单据、库存流水、移动加权平均成本、状态机与边界 |
| 《报价单与PI设计方案.md》 | 报价单 / PI 设计与实现状态（含 §9 完成度清单：已实现 / 浏览器延后 / 仍缺失） |
| 《菜单与业务流程优化建议-20260918.md》 | 菜单蓝图与缺口清单 + 落地状态对照表 |
| 《AUTOMATED_DEVELOPMENT.md》 | 自动化流程、目录与文件职责、运行命令、Human Gate、CI 分层 |
| 《代理服务费协议证据说明.md》 | ERP-069：客户代理服务费协议证据（既有模型审计、条款与校验、身份唯一、状态机、非变更边界、接口 / 界面、幂等结构、测试覆盖） |
| 《代理服务费对账单证据说明.md》 | ERP-070：客户代理服务费对账单证据（显式来源标识符、三方客户 / 币种兼容、重复计费保护、金额与到期日口径、状态机与作废保留、非变更边界、接口 / 界面、幂等结构、测试覆盖） |
| 《供应商对账与账龄工作台说明.md》 | ERP-068：只读派生的供应商对账与账龄工作台（币种分离、未知到期日分组、授权 fail closed） |

## 十三、代理服务费协议证据登记（ERP-069）

**定位**：新增**代理服务费协议证据登记册**，把授权用户**显式提供**的客户代理服务费商业条款（协议号 / 权威客户 /
生效日期区间 / 币种 / 披露的计费方式与显式费率或固定金额 / 有界的计费依据说明 / 有界备注）登记为可审计证据，
并可显式登记与作废；完整口径见《代理服务费协议证据说明.md》。

| 项 | 说明 |
|---|---|
| 新增前的既有模型审计 | 仓库此前**没有**代理服务费协议的权威模型：`BaseCustomers.CommissionRatio`、销售订单 `CommissionRatio` 列、`BaseSuppliers.RebateRatio` 只是主数据 / 单据上的比例设置；业务员提成表（`/api/reports/sales-commission`，比例取自 `SalesCommissionRate`、按毛利计算）是内部提成报表。三者口径不同，本模块**新增独立证据模型**，不替换 / 不复制 / 不派生任何既有模型 |
| 分层落点 | 领域 `AgencyServiceFeeAgreement`；应用 `AgencyServiceFeeAgreementRules`（纯规则）/ `AgencyServiceFeeAgreementService` / `AgencyServiceFeeAgreementDtos`；接口 `AgencyServiceFeeAgreementController`（`/api/agency-service-fee-agreements`）；持久化 `ErpDbContext` + `SchemaUpgrader` 第 42 段；前端 `js/agency-service-fee-agreements.js` + `modules.js`（客户工具栏 / 行操作） |
| 条款口径 | 费用条款只来自用户显式提交：比例费率必填 `(0, 100]`（4 位小数、0.5 进位）或固定金额必填按币种精度取整后 `> 0`，**两者不得同时填写**；币种必须受支持、金额原币、不做汇率换算；**不**从提成设置、主数据比例、历史订单、自由文本或金额相似度推断 |
| 身份与状态 | 同一「客户 + 规范化协议号」在未作废记录内唯一（草稿占用身份、作废释放身份、重复拒绝）；草稿 → 已登记（冻结条款并写入登记时间与登记人）→ 已作废（原因必填，保留条款 / 快照 / 登记人与时间戳） |
| 边界 | 只读写一张表：不开票 / 不作废发票、不记账或生成凭证 / 收付款 / 结算单、不授权付款、不提供法律意见，也不改写客户 / 订单 / 单证 / 收款 / 发票证据 / 库存 / 费用 / 退税记录；不提供任何账外 / 不披露佣金字段或流程 |
| 验证 | `ERP.UnitTests/AgencyServiceFeeAgreementTests.cs`（39 例）；安全档 Release 构建 0 警告 0 错误、`ERP.UnitTests` 1493/1493 通过；浏览器验收按 `browser_deferred` 延后到 `FINAL-UI-ACCEPTANCE` |

## 十四、代理服务费对账单证据登记（ERP-070）

**定位**：在 ERP-069 协议证据之上新增**代理服务费对账单证据登记册**（表头 + 服务来源引用行），把授权用户**显式提供**的
对账证据（对账单号 / 权威客户 / 币种 / 对账日期 / **可选**到期日 / 服务期间 / 显式关联的已登记协议 /
一条或多条**显式服务来源引用行**）登记为可审计证据并可显式登记与作废；完整口径见《代理服务费对账单证据说明.md》。

| 项 | 说明 |
|---|---|
| 新增前的既有模型审计 | 仓库此前**没有**代理服务费对账单的权威模型：ERP-069 协议证据是**条款**（无对账日期 / 服务期间 / 逐笔来源 / 行证据 / 合计）、客户销项发票证据是**发票口径**、收款单及其引用行是**资金口径**、ERP-068 对账工作台是**只读派生报表**（无新增表）；因此本模块**新增独立证据模型**（表头 + 行两张表），不替换 / 不复制 / 不派生任何既有模型，也不向既有表加列 |
| 分层落点 | 领域 `AgencyServiceFeeStatement` / `AgencyServiceFeeStatementLine`；应用 `AgencyServiceFeeStatementRules`（纯规则）/ `AgencyServiceFeeStatementService` / `AgencyServiceFeeStatementDtos`；接口 `AgencyServiceFeeStatementController`（`/api/agency-service-fee-statements`）；持久化 `ErpDbContext` + `SchemaUpgrader` 第 43 段；前端 `js/agency-service-fee-statements.js` + `modules.js`（客户工具栏 / 行操作） |
| 来源链接口径 | 行只按**来源类型 + 来源记录 Id（持久化标识符）**显式引用既有来源：`sales-order`（销售订单：客户与币种都校验）与 `loading-list`（装柜清单：客户按兼容主客户字段校验，**不携带币种** ⇒ 币种校验不适用且照实标注）；**绝不**按单号文本、金额、日期或相似度匹配，也不提供「自动关联 / 推荐来源」入口；不存在的来源 / 已删除来源 / 已取消来源一律拒绝 |
| 三方兼容性 | 必须**显式**关联一条**已登记**的 ERP-069 协议（草稿 / 已作废拒绝），且协议客户 = 对账单客户、协议币种 = 对账单币种；每条行的来源客户（及来源自带币种）必须与对账单一致，不一致即拒绝（不做汇率换算、不跨客户合并） |
| 行证据与合计 | 行说明 / 计费基础说明必填有界，计费基础数量可选（留空 = 未知），行金额按币种精度取整后必须 > 0；**合计由服务端按已校验行求和**，请求模型无任何合计字段；**不**从协议费率 / 固定金额、客户账期与默认值、来源单据金额、自由文本或相似度折算任何费用 |
| 到期日口径 | `DueDate` 是**可选**显式证据：留空 = **未知**（保持 `NULL`，不按客户 `CreditDays`、协议文字或对账日期推算），填写时只校验不得早于对账日期 |
| 唯一性与重复计费 | 同一「客户 + 规范化对账单号」在未作废对账单内唯一（草稿占用身份、作废释放身份、重复拒绝）；**同一服务来源只能被一条未作废对账单行引用**（`UX_AgencyServiceFeeStatementLines_ActiveSource` 过滤唯一索引 + 服务层预检），草稿行同样占用来源身份、作废后释放；重复一律拒绝而不是合并 |
| 状态与留痕 | 草稿（可编辑；替换行时原草稿行**软删除**，不物理删除）→ 已登记（表头与全部行冻结、按持久化行金额重算合计、登记人按已认证身份写入）→ 已作废（原因必填、同步作废全部有效行并释放来源身份、保留原始行与全部快照） |
| 边界 | 只读写两张表：不开票 / 不作废发票、不报税、不记账或生成凭证、不收款或付款、不催收或联系客户、不调用任何外部服务，也不改写协议 / 客户 / 订单 / 装柜清单 / 单证 / 发票证据 / 收款 / 库存 / 费用 / 退税 / 结算记录 |
| 验证 | `ERP.UnitTests/AgencyServiceFeeStatementTests.cs`（50 例：显式链接、三方兼容、重复计费保护、金额精度与服务端合计、未知到期日、登记与作废历史、有界查询与非变更、模型与前端契约）；安全档 Release 构建 0 警告 0 错误、`ERP.UnitTests` 1543/1543 通过；浏览器验收按 `browser_deferred` 延后到 `FINAL-UI-ACCEPTANCE` |




